{
  "name": "world-heritage-app",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "prepare": "husky",
    "dev": "concurrently --kill-others-on-fail --names backend,frontend --prefix-colors blue,green \"npm --prefix backend run start:dev\" \"npm --prefix frontend run dev\"",
    "lint": "npm --prefix frontend run lint && npm --prefix backend run lint"
  },
  "devDependencies": {
    "concurrently": "^10.0.5",
    "husky": "^9.1.7"
  }
}
